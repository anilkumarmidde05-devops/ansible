- name: Conditions
  hosts: local
  connection: local
  vars:
    IS_Live: false
  tasks:
    - name: checking Conditions
      ansible.builtin.debug:
        msg: "Yes, it is live"
      when: IS_Live
