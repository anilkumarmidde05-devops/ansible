- name: Conditions
  hosts: local
  connection: local
  vars:
    IS_Live: true
  tasks:
    - name: checking Conditions
      ansible.builtin.debug:
        msg: "Yes, it is live"
      When: IS_Live
