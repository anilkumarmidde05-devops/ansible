- name: demo on filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, ansible, teraform
  tasks:
    - name: greetins
      ansible.builtin.debug:
        msg: "Hi Sivakumar, {{ GREETINGS | default('Good Morning')}}"

    - name: convert string to list
      ansible.builtin.debug:
        msg: "{{ topics | split(',') }}"
