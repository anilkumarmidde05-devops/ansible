- name: Task level variables
  hosts: local
  connection: local
  vars_files:
    - course.yml
  tasks:
    - name: print vairables
      vars:
        course: Ansible
      ansible.builtin.debug:
        msg: "Course is {{course}} and the trainer is {{Trainer}}"
